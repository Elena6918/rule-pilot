# RulePilot dependencies
streamlit>=1.30
requests>=2.31
python-dotenv>=1.0
urllib3>=2.0

# Splunk MCP Server integration (enables the "Splunk AI Assistant" provider
# and the sidebar MCP connection test). Optional — the rest of RulePilot runs
# without these, but they are small and recommended.
mcp>=1.0
httpx>=0.27
