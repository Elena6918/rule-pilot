SPLUNK_HOST=localhost
SPLUNK_WEB_PORT=8000
SPLUNK_API_PORT=8089
SPLUNK_USERNAME=elena
SPLUNK_PASSWORD=changeme
SPLUNK_INDEX=security
SPLUNK_VERIFY_SSL=false

# --- Local LLM provider (Ollama / OpenAI-compatible) ---
RULEPILOT_MODEL_PROVIDER=openai_compatible
RULEPILOT_MODEL_BASE_URL=http://localhost:11434/v1
RULEPILOT_MODEL_NAME=qwen2.5:7b
RULEPILOT_MODEL_API_KEY=local

# --- Splunk MCP Server (Splunkbase app #7931) ---
# Copy the endpoint from the Splunk MCP Server app UI ("Endpoints" section).
# Generate the token via "Create MCP Encrypted Token" in the same app.
SPLUNK_MCP_ENDPOINT=https://localhost:8089/services/mcp
SPLUNK_MCP_TOKEN=changeme
